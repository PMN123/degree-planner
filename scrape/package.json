{
  "name": "boilerplanner-scrape",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Headless-browser helper that mints an AWS-WAF token cookie so the Python scraper can fetch Purdue catalog program pages.",
  "scripts": {
    "mint": "node waf_fetch.mjs mint"
  },
  "dependencies": {
    "playwright": "^1.47.0"
  }
}
